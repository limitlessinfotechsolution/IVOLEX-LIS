{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "npm run build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}