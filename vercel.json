{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "npm run build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/[^.]+",
      "dest": "/index.html"
    }
  ]
}